#include<bits/stdc++.h>
using namespace std;
int main(){
    list<int>ls;
    //pushfront
    //pushback
    //popfront
    //popback
    //begin end rbegin rend
    //size
    //clear
    //empty
    //at
    //remove big O(1)
    ls.push_front(1);
    ls.push_front(2);
    ls.push_front(3);
    ls.remove(2);//big O (1);
}
